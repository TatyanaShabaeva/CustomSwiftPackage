import XCTest
@testable import CustomSwiftPackage

final class CustomSwiftPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CustomSwiftPackage().text, "Hello, World!")
    }
}
